# Aliases
alias ls='ls -p --color=auto'
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'
alias grep='grep --color=auto'
alias ip='ip -color=auto'
alias rm='echo "This is not the command you are looking for."; false'

# Prompt
export PROMPT='%n@%m %D %T %~%# '

